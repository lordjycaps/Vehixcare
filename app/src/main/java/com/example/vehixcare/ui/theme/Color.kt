package com.example.vehixcare.ui.theme

import androidx.compose.ui.graphics.Color


// Colores para el modo oscuro
val DarkPrimary = Color(0xFF1CFFA8) // Para el color primario en el modo oscuro
val DarkSecondary = Color(0xFFEEEFEF) // Para el color secundario en el modo oscuro
val DarkTertiary = Color(0xFFFCFDFC) // Para el color terciario en el modo oscuro

// Colores para el modo claro
val LightPrimary = Color(0xFF2DC399) // Para el color primario en el modo claro
val LightSecondary = Color(0xFFF0EDD7) // Para el color secundario en el modo claro
val LightTertiary = Color(0xFF282F32) // Para el color terciario en el modo claro
